version: "1.0"
registry: TEMPLATE_REGISTRY
templates: []

# Templates will be registered here with stable TPL-XXX IDs.
# A template may reference approved brand assets, category IDs, and production rules.
